<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Proje" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

  </head>
  <body>



<div id="user_nav">
  
</div>




    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
              <%= link_to "Proje", root_path, :class => 'brand' %>
          <div class="container nav-collapse">
            <ul class="nav">
            <% if current_user %>
              <% if current_user.admin %>
                <li><%= link_to "yeni mesaj", new_message_path  %></li>
                <li><%= link_to "Yeni Kategori", new_category_path  %></li>
              <% end %>
            <% end %>
            </ul>
            <ul class="nav pull-right">
            <% if current_user %>
            <div class="boyut"> Logged in as <%= current_user.try(:name) %>.
              <%= link_to "Çıkış Yap", log_out_path %></div>
            <% else %>
              <%= form_tag sessions_path do %>
              <li> <%= text_field_tag :email, params[:email], :placeholder => "E-Posta", :class => 'span2' %>
                   <%= password_field_tag :password, "", :placeholder => "Parola", :class => 'span2' %>
<%= submit_tag "Log in", :class => "btn btn-primary boyut" %>
                 <%= link_to "Üye Ol", sign_up_path, :class => "btn btn-primary boyut" %> </li>

              <% end %>
            <% end %>
            </ul>
          </div><!--/.nav-collapse -->
            

        </div>
      </div>
    </div>

    <div class="container">
        <div class="row">
          <div class="span12">
            <%= bootstrap_flash %>
            <%= yield %>
          </div><!--/span-->
        </div><!--/row-->

      <footer>
        <p>&copy; Company İpos Bilişim A.Ş 2013</p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
